# # Configure the bash shell using Omakub defaults
# [ -f "~/.bashrc" ] && mv ~/.bashrc ~/.bashrc.bak
# cp ~/.local/share/mint-setup/configs/bashrc ~/.bashrc

# Load the PATH for use later in the installers
# source ~/.local/share/mint-setup/defaults/bash/shell

# [ -f "~/.inputrc" ] && mv ~/.inputrc ~/.inputrc.bak
# # Configure the inputrc using mint-setup defaults
# cp ~/.local/share/mint-setup/configs/inputrc ~/.inputrc

# copy bash_aliases
cp ~/.local/share/mint-setup/dotfiles/.bash_aliases ~/